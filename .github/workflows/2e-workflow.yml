name: "Workflow de folie"
on: 
  issues:
    types: 
      - opened
jobs : 
  job1:
    name : "echoo"
    runs-on : ubuntu-latest
    steps :
      - run : echo -e "Nouvelle issue - ${{ github.event.issue.title}}\n${{ github.event.issue.html_url }}"


